import { Loading as DotLoading } from "@/ui/dotLoading";

export default function Loading() {
  return (
    <div className='flex items-center justify-center w-screen h-screen'>
      <DotLoading />
    </div>
  );
}
